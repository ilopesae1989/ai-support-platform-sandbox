[pytest]
addopts = --import-mode=importlib -m "not live and not local_container"
markers =
    live: tests de integración que llaman a servicios Azure/Foundry reales
    local_container: tests locales que requieren Docker y una imagen local certificada
